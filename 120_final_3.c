#include <stdio.h>
#include <conio.h>
int main()
{
    int row,colum=0,sum,sum1,sum2,flag=0;

    scanf("%d",&size);

    for(row=0;row<size;row++)
    {
        for(colum=0;colum<size;colum++)
            scanf("%d",&)
    }



}

